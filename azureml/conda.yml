name: mcucoder
channels:
  - conda-forge
dependencies:
  - python=3.10
  - pip
  - pip:
      - torch
      - torchvision
      - torchaudio
      - torchmetrics
      - compressai
      - pillow
      - opencv-python
      - numpy
      - tqdm
      - matplotlib
      - datasets
      - huggingface_hub